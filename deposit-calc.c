#include <stdlib.h>
#include <stdio.h>

int main()
{
    printf("Amount of deposits: ");
    scanf("%d", &d);

    printf("Term: ");
    scanf("%d", &g);

    if ((d >= 10000) && (g <= 365) && (g > 0)) {

    return 0;
}
